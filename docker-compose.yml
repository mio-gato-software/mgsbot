services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env:ro
      - ./memory:/app/memory
      - ./audios:/app/audios
      - ./logs:/app/logs
