services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./memory/short-term:/app/memory/short-term
      - ./memory/long-term.json:/app/memory/long-term.json
      - ./memory/members.json:/app/memory/members.json
      - ./audios:/app/audios
